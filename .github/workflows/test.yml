# Generated by Workflow Builder (https://githubworkflow.com)
# This workflow was created using our visual workflow builder

name: Test Workflow
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment
        required: true
        default: staging
        type: choice
        options:
        - development
        - staging
        - production
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Run a one-line script
      run: echo Hello, everyone!
